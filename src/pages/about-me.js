import React from "react";
import Layout from "../layout/layout";

const About = () => {
    return (
        <Layout>
            <p>This is a test.</p>
        </Layout>
    );
};

export default About;
