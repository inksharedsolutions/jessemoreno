import React from "react";
import Layout from "../layout/layout";

const Contact = () => {
    return (
        <Layout>
            <p>This is a test.</p>
        </Layout>
    );
};

export default Contact;
